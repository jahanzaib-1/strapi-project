'use strict';

/**
 * mission-statement-card controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::mission-statement-card.mission-statement-card');
