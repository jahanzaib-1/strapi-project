'use strict';

/**
 * contact-information-card router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::contact-information-card.contact-information-card');
