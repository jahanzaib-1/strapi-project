'use strict';

/**
 * browse-articles-card controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::browse-articles-card.browse-articles-card');
